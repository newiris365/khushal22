[build]
  command = "npm run build"
  publish = ".next"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "production"
  NODE_OPTIONS = "--max_old_space_size=4096"

# @netlify/plugin-nextjs enables full SSR support including:
# - Server-side cookie access (required for PKCE code_verifier)
# - API Routes running as Netlify Functions
# - Incremental Static Regeneration (ISR)
[[plugins]]
  package = "@netlify/plugin-nextjs"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=(self)"

[[redirects]]
  from = "/api/settings/*"
  to = "/.netlify/functions/settings/:splat"
  status = 200

[[redirects]]
  from = "/api/v1/*"
  to = "https://iris-backend-hkjv.onrender.com/api/v1/:splat"
  status = 200
  force = true
